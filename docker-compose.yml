# PRODUCTION DOCKER COMPOSE FILE
services:
  db:
    image: mongo:7.0
    container_name: stocker_mongo
    restart: unless-stopped
    env_file: .env
    environment:
      MONGO_INITDB_ROOT_USERNAME: ${MONGO_USER}
      MONGO_INITDB_ROOT_PASSWORD: ${MONGO_PASSWORD}
      MONGO_INITDB_DATABASE: ${MONGO_DB_NAME}
    volumes:
      - mongo_data:/data/db
    healthcheck:
      test: ["CMD", "mongosh", "--eval", "db.adminCommand('ping')"]
      interval: 10s
      timeout: 10s
      retries: 5
      start_period: 5s

  backend:
    build:
      context: ./app/backend
    container_name: stocker_backend
    restart: unless-stopped
    env_file: .env
    command: >
      sh -c "python manage.py migrate &&
             python manage.py create_default_users &&
             gunicorn core.wsgi:application --bind 0.0.0.0:8000 --workers 3 --timeout 60"
    volumes:
      - media_data:/app/media
    depends_on:
      db:
        condition: service_healthy

  frontend:
    build:
      context: ./app/frontend
      target: prod
    container_name: stocker_frontend
    restart: unless-stopped

  nginx:
    image: nginx:1.30.0-alpine
    container_name: stocker_nginx
    restart: unless-stopped
    ports:
      - "${WEB_PORT:-2026}:80"
    volumes:
      - ./app/nginx/nginx.prod.conf:/etc/nginx/conf.d/default.conf:ro
      - media_data:/media:ro
    depends_on:
      - backend
      - frontend

volumes:
  mongo_data:
  media_data:
